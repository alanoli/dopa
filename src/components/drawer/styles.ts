import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    container: {
        position: 'absolute',
        top: '2rem',
        right: '2rem'
    }
}))