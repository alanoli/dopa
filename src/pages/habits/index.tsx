import React from 'react';
import ScreenHeader from '../../components/screenHeader/ScreenHeader';

const Habits = () => {
    return (
        <div>
            <ScreenHeader title={'Hoje'} subtitle={'Hábitos'} />
        </div>
    )
}

export default Habits;